# Production-safe default. Mock data is available only in Vite development mode
# and only when this flag is changed to the exact value "true".
VITE_USE_MOCK_API=false
VITE_API_BASE_URL=
# Comma-separated exact HTTPS hostnames for production payment gateways.
VITE_PAYMENT_ALLOWED_HOSTS=
